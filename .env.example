# Optional: enables AI classification, minutes, and proposal generation.
# Configure the real value in Netlify environment variables. Never commit it.
ANTHROPIC_API_KEY=

# Optional: override the default Anthropic model.
AI_MODEL=claude-opus-4-8
